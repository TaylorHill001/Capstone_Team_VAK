aaa,200,1

#Extra code to possibly use

#Score Retrival
# pygame.font.init()
# font = pygame.font.Font('Consolas', 32)
# fontX = 15
# fontY = 15

# def load_scores(filename):
#     scores = []
#     try:
#         with open(filename, "r.txt") as file:
#             for line in file:
#                 parts = line.strip().split(",")
#                 if len(parts) == 3:
#                     name, score, level = parts
#                     scores.append((name, int(score), int(level)))  # Convert score and level to integers
#     except FileNotFoundError:
#         print("Score file not found. Creating a new one.")
#     return sorted(scores, key=lambda x: x[1], reverse=True)  # Sort by score (highest first)

# #Score Save
# def save_score(filename, name, score, level):
#     with open(filename, "a") as file:  # Append mode to add new scores
#         file.write(f"{name},{score},{level}\n")